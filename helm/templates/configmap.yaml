---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lvminit.fullname" . }}-config
  labels:
    {{- include "lvminit.labels" . | nindent 4 }}
data:
  config.yaml: |
{{- .Values.config | toYaml | nindent 4 }}
